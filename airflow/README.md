# Phase 3 — Airflow: Local DAG (Orchestrator Comparison)

Coming soon.

The same ingestion pipeline rebuilt as a local Airflow DAG (Docker) —
a comparison exercise against Azure Data Factory, not a second production
system. Includes documented tradeoffs (ADF vs. Airflow) in the project README.
