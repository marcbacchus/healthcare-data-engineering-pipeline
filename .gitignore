# Terraform — never commit secrets or local state
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/terraform.tfvars

# Python
__pycache__/
*.pyc
.env
.venv/

# Data files (can be large)
data/
*.csv
*.parquet

# dbt
dbt/target/
dbt/logs/
dbt/.user.yml

# macOS
.DS_Store

# Local working notes and project instructions (not portfolio content)
notes.txt
notes/*.md
CLAUDE.md

# Synthea
ingest/synthea-with-dependencies.jar
ingest/output/
