# Phase 3 — Azure Orchestration: ADF, ADLS Gen2, Key Vault, Snowpipe

Coming soon.

Covers ADLS Gen2 landing zone, Key Vault (managed identity), Azure Data
Factory pipeline (HTTP source → ADLS → Snowflake external stage →
Snowpipe/COPY INTO), scheduled trigger, Azure Monitor alerting, and ARM
template export for full infra reproducibility.
